{
  "name": "station-selector",
  "scripts": {
    "build": "tailwindcss build -i main.css -o styles.css --minify",
    "watch": "tailwindcss build -i main.css -o styles.css --watch --minify"
  },
  "devDependencies": { "@tailwindcss/forms": "^0.5.7", "tailwindcss": "^3.4.4" }
}
